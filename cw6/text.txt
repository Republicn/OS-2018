readelf -d - выдаёт зависимости с библиотеками
elf - формат исполняемых файлов в линукс
_ - символ, с которого начинается компилирование в программе
&(CC) - компилятор $< - вместо этой переменной поставится имя переменной $@
ldd - от чего зависит
динамическая линковка - без подгружения, только во время компиляции
delopen - открыть библиотеку и поработать с ней
.so для динамических библиотек, .а - для статических
man ar - утилита для архивирования 
gcc -c не вызывать линковщик
-shared - собираем динамическую библиотеку(с помощью релокейтов или чего-то другого) -fpic - Position depended код
-L - где искать библиотеки -I - где дополнительно поискать -Wl - следующие ключи возьми и передай инковщику - rpath - способ сказать рантайм загрузчику,
что нужно на этом пути искать билиотеку для рантаймлоадера -hello - ъотим динамически связать с библиотекой hello
http/1.1 & 1.0- текстовые страницы
Транспортные протоколы: dns - преобразование доменных имён в адреса. TCP гарантирует, что все данные дойдут вправильном порядке. Гарантирует
правильное получение даже в случае ошибки. Проблема TCP - скорость передачи